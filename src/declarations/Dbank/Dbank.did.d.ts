import type { Principal } from '@dfinity/principal';
export interface _SERVICE {
  'add' : (arg_0: bigint) => Promise<undefined>,
  'checkbalance' : () => Promise<bigint>,
  'subtract' : (arg_0: bigint) => Promise<undefined>,
}
